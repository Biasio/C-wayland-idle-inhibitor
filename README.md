wayland-idle-inhibitor
======================
Fork of stwa's python script written in C.

It's an inhibitor that prevents wayland from idleing and locking the screen.
Can be killed with pkill.

To build simply run make
and to install sudo make install
